--- src/events.rs
+++ delete match arm KeyCode::Char('n') | KeyCode::Char('N') | KeyCode::Esc in handle_confirm_mode
@@ -86,20 +86,17 @@
 }
 
 fn handle_confirm_mode(app: &mut App, key: KeyEvent) -> bool {
     match key.code {
         KeyCode::Char('y') | KeyCode::Char('Y') => {
             app.execute_kill();
             false
         }
-        KeyCode::Char('n') | KeyCode::Char('N') | KeyCode::Esc => {
-            app.cancel_modal();
-            false
-        }
+         /* ~ changed by cargo-mutants ~ */
         _ => false,
     }
 }
 
 #[cfg(test)]
 mod tests {
     use crate::app::App;
     use crate::ports::PortInfo;
